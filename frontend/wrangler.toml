# Cloudflare Pages configuration
name = "1-1-by-namex-frontend"
compatibility_date = "2024-01-01"

# Pages configuration
pages_build_output_dir = "dist"

[env.production]
vars = { ENVIRONMENT = "production" }

[env.staging]
vars = { ENVIRONMENT = "staging" }
